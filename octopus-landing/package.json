{
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "verify": "npm test -- --run && npm run test:e2e && npm run build:draft",
    "verify:release": "npm test -- --run && npm run test:e2e && npm run build",
    "build": "node scripts/validate-production-env.mjs && tsc -b && vite build && npm run audit:dist",
    "build:draft": "node scripts/validate-production-env.mjs --draft && tsc -b && vite build && npm run audit:dist:draft",
    "build:pages-preview": "node scripts/validate-production-env.mjs --draft && tsc -b && vite build --base=/octopus-ai-preview/ && node scripts/audit-dist.mjs --draft --base=/octopus-ai-preview/ && node scripts/prepare-pages-preview.mjs",
    "build:local": "npm run build:draft",
    "audit:dist": "node scripts/audit-dist.mjs",
    "audit:dist:draft": "node scripts/audit-dist.mjs --draft",
    "preview": "vite preview",
    "test:e2e": "env -u NO_COLOR playwright test"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@testing-library/jest-dom": "^6.6.4",
    "@testing-library/react": "^16.3.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  }
}
